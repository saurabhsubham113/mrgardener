import React from "react";
import spinner from "../assets/icons/spin.gif";

const Spinner = () => {
  return (
    <div className="">
      <img src={spinner} alt="loading" />
    </div>
  );
};

export default Spinner;
